---
plugin: nutanix.ncp.ntnx_prism_vm_inventory
# The IP or FQDN of your Prism Central
nutanix_host: "10.20.24.5" 
nutanix_username: "michael"
# It is better to use an AWX credential for this, but for testing:
nutanix_password: "Oba@2024" 
validate_certs: false

# Your original filters/groups
include_vms_with_power_state:
  - 'on'
compose:
  ansible_host: address_ips[0]
keyed_groups:
  - prefix: nutanix_vlan
    key: subnet_name
    separator: "_"
