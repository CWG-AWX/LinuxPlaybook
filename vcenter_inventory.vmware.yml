plugin: vmware.vmware.vms
strict: False
validate_certs: False
with_hostname: True
# Ensure this matches your vCenter IP
host: 192.168.92.101

# This section helps group your VMs in AWX
keyed_groups:
  - key: runtime.powerState
    prefix: state
  - key: config.guestId
    prefix: os
